void shuffleArray(int arr[],int n)
	{
	    vector<int> v;
	    int k = n/2;
	    for(int i=0;i<n/2;i++)
	    {
	        v.push_back(arr[i]);
	        v.push_back(arr[k]);
	        k++;
	    }
	    for(int i=0;i<n;i++)
	    {
	        arr[i] = v[i];
	    }
	}
